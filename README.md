# CryptoBotMonitor-

first use:
pip install aiogram
pip install ccxt
pip install asyncio
pip install os
pip install time

Change api_key, secret_key, API_TOKEN to your tokens

channel_id to your channel id

